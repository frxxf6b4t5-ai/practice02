고정형: 브라우저 가로 크기에 변화가 없는 형태 
	   단위 px

적응형: 브라우저 가로 크기에 따라 적응하는 형태  -> 속성값 변화가 없다
	  (크기변화) /단위  %

반응형 : 브라우저의 가로 크기에 따라 반응하는 형태  ->속성값 변화 있다
	   (레이아웃 변화)/ 단위 px, %


- 네이티브앱 (맥)
- 하이브리드앱/웹앱/반응형웹/적응형웹/모바일웹

-1. 명품 홈페이지
	OTO(Online to Offline/ex.쿠팡, 마켓컬리), 웨어러블(ex.워치), IOT(Internet of Things/ex.휴대폰으로 집 불 켜기->끊기지 않는 통신 필요) ==> 웹
0. 키오스크 / QR / 탭 (편한 주문)
1. 디지털 교과서 / 전자 교과서
2. XR -> VR (없는 걸 있는 것처럼 보이는 것/행동은 불가능)+ AR (없는 데 있는 것처럼 행동하는 것) // 애플비전

CSS속성 작성하는 순서
: 실사이즈, 여백, 배치, 배경, 타이포그래피, 특수성 // 부모먼저, 형먼저!